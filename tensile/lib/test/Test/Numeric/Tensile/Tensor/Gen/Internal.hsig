signature Test.Numeric.Tensile.Tensor.Gen.Internal (gen_tensor') where

import Numeric.Tensile.Dimensions (Dims(..))
import Numeric.Tensile.Tensor (Tensor(..), Elt(..))
import Hedgehog

gen_tensor' :: (Elt e, MonadGen m) => Dims d -> m e -> m (Tensor d e)
