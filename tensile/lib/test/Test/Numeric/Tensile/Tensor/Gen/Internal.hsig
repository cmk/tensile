signature Test.Numeric.Tensile.Tensor.Gen.Internal where

import Numeric.Tensile.Tensor (Tensor(..), Elt(..))
import Numeric.Tensile.Types (Dims(..))

import Hedgehog

gen_tensor' :: (MonadGen m, Elt e) => Dims d -> Range e -> (Range e -> m e) -> m (Tensor d e)
